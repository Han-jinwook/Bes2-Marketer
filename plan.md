지금 작성된 `logic.py`의 Gemini 시스템 프롬프트(System Prompt) 부분을 더 강력하게 수정해줘.
기능 구현은 잘 되었는데, 우리 프로젝트의 '철학'과 '취지'가 마케팅 문구에 더 짙게 묻어나야 해.

프롬프트 내용을 아래 [핵심 철학]을 반영해서 업데이트해줘:

[핵심 철학 및 취지]
1. **Underdog 정신:** 수많은 유료 앱과 거대 클라우드 기업들이 사용자의 데이터를 서버로 가져가고 돈을 요구할 때, 우리는 '오직 사용자 편'에 선 무료 & 온디바이스 앱이다.
2. **Privacy First:** "당신의 추억(사진)은 당신의 폰 안에만 있어야 한다."는 강력한 보안 철학을 강조해라. 서버 전송이 아예 없다는 점을 안심 포인트로 어필해라.
3. **Smart Backdoor Strategy:** 구글포토나 클라우드를 쓰지 말라는 게 아니다. "쓰레기 사진까지 백업해서 돈 낭비하지 말고, Bes2로 알맹이만 남긴 뒤 똑똑하게 백업하라"는 현실적이고 경제적인 솔루션을 제시해라.

AI가 이메일이나 댓글을 생성할 때, 단순 홍보가 아니라 **"구독자들의 돈과 개인정보를 아껴주는 진정성 있는 제안"**처럼 들리도록 어조를 다듬어줘.

---

## Update: 2025-12-27 - 검색 엔진 고도화 및 CRM 기능 탑재

### 1. 검색 엔진 고도화 (`logic.py`)
- **이메일 헌팅 강화**:
    - 1차: 영상 설명글(`description`) 파싱
    - 2차: 채널 설명글(`about`) 파싱
    - **3차 (New)**: DB Fallback - 이전에 수집된 채널 정보가 있다면 DB에서 이메일을 불러와 자동 매칭 (재검색 시 효율 극대화).
- **안전 검색 (Safety)**:
    - YouTube API 차단 방지를 위한 랜덤 딜레이(1~3초) 적용.
    - `require_email` 옵션 추가 (UI에서 제어 가능).

### 2. CRM(고객 데이터 관리) 및 UI 개선 (`main.py`)
- **이메일 상태 시각화**:
    - 테이블에 '연락' 컬럼 추가.
    - 📧 (이메일 확보됨): 즉시 발송 가능.
    - 💬 (이메일 없음): 댓글 마케팅 필요.
- **수동 이메일 업데이트 (Human-in-the-loop)**:
    - 💬 상태인 영상을 체크(Select)하면, 하단에 **[이메일 수동 입력창]** 활성화.
    - 입력 시 DB(`leads` 테이블)가 즉시 업데이트되고, 화면 아이콘이 📧로 변경됨.
    - 한 번 입력해두면 향후 해당 채널의 모든 영상 검색 시 자동으로 이메일 매칭.
- **데이터 뷰 통합**:
    - [검색 결과] 모드와 [DB 저장된 영상] 모드를 통합하여 일관된 테이블 UI 제공.
    - 검색 실행 시 결과 데이터가 **즉시 DB에 자동 저장(Upsert)**되도록 하여 데이터 유실 방지.

### 3. 안정성 확보 (Bug Fixes)
- **UI 렌더링 에러 방지**:
    - `KeyError: 'published_at'`, `AttributeError: 'NoneType' object...` 등의 에러를 원천 차단하기 위해 `.get()` 메서드와 예외 처리를 모든 필드에 적용.
- **서버 캐시 대응**:
    - Streamlit Cloud의 배포 지연 문제 해결을 위한 강제 업데이트 트리거 적용.